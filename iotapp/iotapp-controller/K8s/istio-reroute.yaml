apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: gwf1-to-encoder
spec:
  hosts:
  - iotapp-gwi
  http:
  - match:
    - sourceLabels:
        app: iotapp-gwf1
    route:
    - destination:
        host: iotapp-encoder
        port:
          number: 9100
