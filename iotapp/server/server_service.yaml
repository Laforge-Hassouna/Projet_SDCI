apiVersion: v1
kind: Service
metadata:
  name: iotapp-server
spec:
  selector:
    app: iotapp-server
  type: ClusterIP
  ports:
  - name: server-port
    protocol: TCP
    port: 8080
    targetPort: 8080
